import React from 'react'

const Palette = () => {
    return (
        <div className="palette">
            <input type="color" />
            <button>Click</button>
        </div>
    )
}

export default Palette
